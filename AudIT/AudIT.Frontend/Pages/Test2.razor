@page "/page"
@using Radzen.Blazor.Rendering

<RadzenButton @ref="btnRef" Click="@(args=>popupRef.ToggleAsync(btnRef.Element))"/>

<Popup  @ref="@popupRef"
        Style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; display: none;"
       >
           <RadzenCard Variant="Variant.Filled">
               <RadzenText Text="Text"></RadzenText>
               <RadzenText Text="Text"></RadzenText>
               <RadzenText Text="Text"></RadzenText>
           </RadzenCard>
</Popup>

@code
{
    bool visible = true;
    RadzenButton btnRef;

    Popup popupRef;
}
